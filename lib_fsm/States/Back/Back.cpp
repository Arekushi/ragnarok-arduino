#include <Back.h>
#include <Arduino.h>
#include <Car.h>

void Back::enter(Car data) {
    base::enter(data);
}

void Back::exit(Car data) {
}

void Back::setActions() { 
}

void Back::setTransitions() {
}
